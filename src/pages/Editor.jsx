import { Link } from "react-router";

export const Editor = () => {
    return (
        <div>
            <h1>Editor Page</h1>
            <Link to="/">Go back to Landing</Link>
        </div>
    );
}