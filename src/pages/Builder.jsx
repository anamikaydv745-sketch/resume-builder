import { Link } from "react-router";

export const Builder = () => {
    return (
        <div>
            <h1>builder Page</h1>
            <Link to="/">Go back to Landing</Link>
        </div>
    );
}